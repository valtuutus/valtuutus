namespace Authorizee.Core;

public class LookupEngine
{
    public async Task<List<string>> LookupEntity(LookupEntityRequest req, CancellationToken ct)
    {
        List<string> entityIDs = [];

        return entityIDs;
    }
}